// Configuration file for API endpoint
// Copy this to config.js and update with your deployment URL

const CONFIG = {
  // For Render deployment (frontend + backend same server): use ""
  // For local development: use "http://localhost:5000"
  // For separate frontend/backend: use your backend URL (e.g., "https://your-app.onrender.com")
  API_URL: ""
};

